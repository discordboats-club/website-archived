<!DOCTYPE html>
<html>
    <head>
        <%- include("../parts/head") %>
    </head>
    <body>
        <div class="header">
            <%- include("../parts/header") %>
        </div>
        <div class="main">
            <div class="container">
                <% if (rawBots.length == 0) { %>
                    <h4>You have no bots! <a href="/dashboard/new">Add one?</a></h4>
                <% } else { %>
                    <div class="bot-cards">
                            <!-- Bot cards includes can go here -->
                        <%- myBots.map(chunk => `<div class="row">${chunk.map(bot => include("../parts/botCard", {bot}))}</div>`) %>
                    </div>
                <% } %>
            </div>
        </div>
    </body>
    <footer>
        <%- include("../parts/footer") %>
    </footer>
</html>